header(class={header: true, "header--logged": user})
  a.header__logo.logo(href='/')
    img.logo__image(src='/img/icons/logo-image-only.svg' alt='логотип Типотеки')
    span.logo__name Типотека
  nav.header__nav
    ul.header__nav-list
      if user
        li.header__list-item
          a.header__registration(href='/my') Мои записи
        li.header__list-item
          a.header__enter(href='/articles/add') Добавить пост
      else
        li.header__list-item
          a.header__registration(href='/register') Регистрация
        li.header__list-item
          a.header__enter(href='/login') Вход с паролем
  a.header__search.button.button--search(href='/search' aria-label='поиск по сайту')
  if user
    a.header__avatar(href="/my")
      img(src=`/img/${user.avatar ? user.avatar : "avatar-1.png"}` alt="Аватар пользователя")
    a.header__enter(href="/logout") Выход
